{
  "name": "gcp-hello-world-function",
  "version": "0.1.0",
  "private": true,
  "description": "Hello World GCP Cloud Function (TypeScript)",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "functions-framework --target=helloWorld --source=lib",
    "dev": "npm run build && npm run start",
    "clean": "rm -rf lib",
    "deploy": "npm run build && gcloud functions deploy helloWorld --gen2 --runtime=nodejs22 --region=europe-west2 --entry-point=helloWorld --trigger-http --no-allow-unauthenticated --set-secrets \"GEMINI_API_KEY=projects/testproject-487517/secrets/GEMINI_API_KEY:latest\""
  },
  "dependencies": {
    "@google-cloud/functions-framework": "3.5.1",
    "@mozilla/readability": "0.6.0",
    "dotenv": "16.6.1",
    "jsdom": "28.1.0"
  },
  "devDependencies": {
    "@types/express": "4.17.25",
    "@types/jsdom": "27.0.0",
    "@types/node": "20.19.33",
    "typescript": "5.9.3",
    "vitest": "4.0.18"
  }
}
